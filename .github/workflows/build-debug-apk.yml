
name: Build Flutter Debug APK

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup Flutter with Android
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'
          android: true

      # # 3️⃣ Generate google-services.json from secret
      # - name: Generate Google Services JSON
      #   run: |
      #     mkdir -p android/app
      #     echo '${{ secrets.GOOGLE_SERVICES_JSON }}' > android/app/google-services.json

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # 5️⃣ Accept Android licenses
      - name: Accept Android Licenses
        run: yes | flutter doctor --android-licenses

      # # 6️⃣ ⬇️ Download ONNX models from Google Drive
      # - name: Download ONNX Models
      #   run: |
      #     # Install gdown to fetch from Google Drive
      #     pip install -q gdown

      #     # Create model folders
      #     mkdir -p assets/onnx_models/cebuano_to_tagalog
      #     mkdir -p assets/onnx_models/tagalog_to_cebuano

      #     echo "⬇️ Downloading Cebuano → Tagalog models..."
      #     gdown "https://drive.google.com/uc?id=1275ZVTngGBffqcdz9ZYWCOD948-8AGUg" -O assets/onnx_models/cebuano_to_tagalog/encoder_model.onnx
      #     gdown "https://drive.google.com/uc?id=17WQOSXtFuM_dS_7XcB9ie4sg3ov1-9lg" -O assets/onnx_models/cebuano_to_tagalog/decoder_model.onnx

      #     echo "⬇️ Downloading Tagalog → Cebuano models..."
      #     gdown "https://drive.google.com/uc?id=19UcZrdqQBGvx7whjqsrDH4-Igo6FSRpp" -O assets/onnx_models/tagalog_to_cebuano/encoder_model.onnx
      #     gdown "https://drive.google.com/uc?id=14ROfXr2Ta7Pzr2NtilZj7EGqL2E4z5jl" -O assets/onnx_models/tagalog_to_cebuano/decoder_model.onnx

      # # 7️⃣ ✅ Verify models exist and show their sizes
      # - name: Verify model files
      #   run: |
      #     echo "📂 Cebuano → Tagalog:"
      #     ls -lh assets/onnx_models/cebuano_to_tagalog

      #     echo "📂 Tagalog → Cebuano:"
      #     ls -lh assets/onnx_models/tagalog_to_cebuano

      #     echo "✅ Total model files count:"
      #     find assets/onnx_models -type f | wc -l

      # 8️⃣ Build debug APK
      - name: Build Debug APK
        run: flutter build apk --debug

      # 9️⃣ Upload APK as artifact
      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
